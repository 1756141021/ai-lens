# dependencies
node_modules/
# pnpm store, if a container/CI pnpm ever falls back to the project dir
.pnpm-store/

# container X11 smoke output
.smoke/

# build output
dist/
src-tauri/target/

# design scratch (may contain a personal desktop screenshot)
mockups/

# updater signing keys — the private key must NEVER be committed
.signing/

# editor / OS
.vscode/
.idea/
*.log
.DS_Store
Thumbs.db
